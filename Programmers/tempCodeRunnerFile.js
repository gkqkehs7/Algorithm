
    // while(queue.length > 0) {

    //     let [x, y, cost] = queue.pop();

    //     console.log(x, y, cost)

    //     if(x === hei - 1 && y === wid - 1) {
    //         answer = Math.max(answer, cost);
    //         continue;
    //     }

    //     for(let i=0; i<4; i++) {
    //         let nx = x + dx[i];
    //         let ny = y + dy[i];

    //         if(nx < hei && nx >=0 && ny < wid && ny >= 0 && maps[nx][ny] !== 1) {
    //             queue.push([nx, ny, cost + 1]);
    //         }
            
    //     }
    // }
